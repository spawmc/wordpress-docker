First of all, we must configure the variables for the DBMS and WordPress and modify the dotfiles to our needs, as is the case with the reverse proxy.
## How to deploy? easy:

`docker-compose up -d`

Please note that this is only a template. Ty.
